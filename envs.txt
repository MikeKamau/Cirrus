#Set the following environment variables as key value pairs e.g. MAIL_SERVER=smtp.mail.yahoo.com, MAIL_PASSWORD=blahblahblah e.t.c
#There should be no quotes around the values for the different keys specified below

MAIL_SERVER=
MAIL_PORT=
MAIL_USE_TLS=
MAIL_USERNAME=
MAIL_PASSWORD=
FLASK_ENV=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
BUCKET_NAME=
REGION_NAME=
